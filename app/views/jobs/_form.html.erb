<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
        <%= f.label :title %><br />
        <%= f.text_field :title %>
    </div>
    <div class="field">
        <%= f.label :employer %><br />
        <%= f.text_field :employer %>
    </div>
    <div class="field">
        <%= f.label :status %><br />
        <%= f.text_field :status %>
    </div>
    <div class="field">
        <%= f.label :project %><br />
        <%= f.text_field :project %>
    </div>
    <div class="field">
        <%= f.label :url %><br />
        <%= f.text_field :url %>
    </div>
    <div class="field">
        <%= f.label :start_month %><br />
        <%= f.text_field :start_month %>
    </div>
    <div class="field">
        <%= f.label :end_month %><br />
        <%= f.text_field :end_month %>
    </div>
    <div class="field">
        <%= f.label :short_desc, "Short description" %><br />
        <%= f.text_area :short_desc %>
    </div>
    <div class="field">
        <%= f.label :long_desc, "Long description" %><br />
        <%= f.text_area :long_desc %>
    </div>
    <div class="field">
        <%= f.label :reason, "Reason for leaving" %><br />
        <%= f.text_area :reason %>
    </div>
    <fieldset name="skills">
        <h2>Skills:</h2>
        <textarea name="skills"><%= @skills.join(", ") %></textarea>
    </fieldset>
    <fieldset name="softwares">
        <h2>Software programs:</h2>
        <textarea name="softwares"><%= @softwares.join(", ") %></textarea>
    </fieldset>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
