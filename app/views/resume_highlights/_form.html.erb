<%= form_for(@resume_highlight) do |f| %>
  <% if @resume_highlight.errors.any? %>
    <div class="error-explanation">
      <h2><%= pluralize(@resume_highlight.errors.count, "error") %> prohibited this resume_highlight from being saved:</h2>

      <ul>
      <% @resume_highlight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resume_id %><br />
    <%= f.text_field :resume_id %>
  </div>
  <div class="field">
    <%= f.label :highlight_id %><br />
    <%= f.text_field :highlight_id %>
  </div>
  <div class="actions">
    <%= f.submit "Save", :class => "action-button" %>
  </div>
<% end %>
