<%
  resume ||= nil
  user ||= nil
  if resume
    path = software_path(:user_id => user.id, :resume_id => resume.id, :id => software.slug)
  else
    path = software_path(:user_id => user.id, :id => software.slug)
  end
%>
<a href="<%= path %>" rel="internal" title="See other positions where I used <%= decode(software.title) %>">
    <%= decode(software.title) %></a>